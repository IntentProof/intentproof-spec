# Migration Notes

This template pack has no production migration requirements.

When creating a new reference policy pack from this template:

- Replace the `reference.templates.*` identifiers with the target reference ID.
- Stamp tenant-specific IDs only after forking into a tenant workspace.
- Recompute and sign the policy before activation.
